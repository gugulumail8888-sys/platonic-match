-- =====================================================
-- Stripeの現在の請求期間終了日（次回更新日）
-- サブスクリプションの次回更新日/失効日を保存するために使用する
-- =====================================================

ALTER TABLE public.profiles
  ADD COLUMN current_period_end TIMESTAMPTZ;
