-- =====================================================
-- オプションプラン契約開始日
-- 退会前チェック（オプション契約中チェック）で
-- 有効期限（開始日 + 1ヶ月 - 1日）を算出するために使用する
-- =====================================================

ALTER TABLE public.profiles
  ADD COLUMN subscription_started_at TIMESTAMPTZ;
