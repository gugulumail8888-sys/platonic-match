services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
      args:
        - NEXT_PUBLIC_SUPABASE_URL=https://rffnjcajfbduqtutraeo.supabase.co
        - NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_1tVWEyw5RcICyK74cSwkkg_FMxs0JSD
        - NEXT_PUBLIC_APP_URL=https://amista.net
    container_name: amista_app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    mem_limit: 2g
    networks:
      - konkatsu_network
    restart: unless-stopped

networks:
  konkatsu_network:
    driver: bridge
